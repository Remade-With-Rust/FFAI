# PP-OCRv5 mobile text detector (DBNet / PP-LCNetV3, Apache-2.0, plan §7.1
# audit) — the speed-margin lineage for LIVE. Converted from the paddlex
# cache by tools/carmenta_mobiledet_prepare.py (234 tensors, shape map in
# corpora/refs/fixtures/ppocrv5_mobile_det_shapes.json). Candle port = the
# open campaign brick.
name = "ppocrv5-mobile-det"
task = "ocr"
description = "PP-OCRv5 mobile det (DBNet/PP-LCNetV3) — staged detector lineage"
license = "Apache-2.0"

[[files]]
name = "det.safetensors"
sha256 = "abe92a954293370d2dc078bceae0c316f6c3bd02542c84ff8be166e7b797c97d"

# Training checkpoint (STRUCTURAL names: backbone.blocksN / neck / head —
# the export-fused inference program has only op-indexed names, which
# blocks a direct port). The candle port loads THESE 905 tensors and folds
# the rep-conv branches at load; oracle = mobiledet_fixture.json (polys +
# texts from the mkldnn-off reference on a pinned frame).
[[files]]
name = "det-train.safetensors"
sha256 = "d7188218e5666327c0006ed149a2305da6cedae15132a19c0e657c45fb9df052"
